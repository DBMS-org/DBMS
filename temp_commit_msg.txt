refactor(stores): simplify store model and remove security level

Remove security level field and related enum from store model. Simplify store interface by removing unnecessary fields like storeType, inspection dates, and audit fields. Update components and services to reflect these changes. Also clean up mock data and validation services.